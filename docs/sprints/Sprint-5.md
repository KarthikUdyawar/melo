# Melo — Sprint 5: Observability & Monitoring

**Duration:** Week 5
**Goal:** Ship full three-pillar observability (logs, metrics, traces) + continuous profiling + Telegram alerting + Streamlit admin — all provisioned as code, all starting with `docker compose up`.
**Branch strategy:** `develop` base → feature branches → PR → CodeRabbit review → merge

---

## Sprint Goal

> *Know what broke, when, and why — without touching the terminal.*

---

## Why This Stack

- **Grafana** as single pane of glass — dashboards, alerting, trace drill-through, profiling, all in one UI
- **Promtail + Loki** over Docker log driver — structured label extraction from JSONL; decouples log format from transport
- **OTEL auto-instrumentation** at startup — zero per-endpoint decoration; `trace_id` in every log line enables log ↔ trace correlation
- **Pyroscope continuous profiling** — catches intermittent CPU spikes; low overhead at this scale
- **Gzip + upload on roll** — minimises local disk; no cron; simpler failure model than batch
- **APScheduler cleanup** — runs inside existing API process; no extra container
- **`celery-exporter` sidecar** — Celery metrics with zero worker code changes
- **Postgres + Redis exporters** — DB and broker health without Melo code changes
- **cAdvisor + Node Exporter** — container and host OS visibility; standard in every Prometheus stack

---

## File Structure (additions only)

```text
infra/
  docker-compose.monitoring.yml   # monitoring stack (separate from main compose)
  monitoring.sh                   # up/down/restart/status entrypoint
  setup-infra.sh                  # idempotent config file generator
  prometheus/
    prometheus.yml                # all scrape targets
    minio_token                   # bearer token for MinIO metrics (generated)
  loki/
    loki.yml
  promtail/
    promtail.yml
  tempo/
    tempo.yml
  pyroscope/
    pyroscope.yml
  grafana/
    provisioning/
      datasources/
        all.yml                   # Loki, Prometheus, Tempo, Pyroscope
      dashboards/
        all.yml
        api.json
        pipeline.json
        system.json
      alerting/
        rules.yml                 # all alert rules (relativeTimeRange required on every node)
        telegram.yml              # contact point + notification policy

admin/
  app.py                          # entry point + login gate + sidebar
  auth.py                         # session password check
  pages/
    overview.py
    songs.py
    logs.py
    metrics.py
    alerts.py
    db_health.py                  # postgres + redis exporter stats
  Dockerfile
  requirements.txt

app/
  core/
    log_events.py                 # LogEvent enum — single source of truth
    log_manager.py                # rotation + gzip + MinIO upload + APScheduler cleanup
    logging.py                    # rewrite: dual renderer (stdout string + file JSONL)
    metrics.py                    # all Prometheus metric definitions
    profiling.py                  # Pyroscope configure_pyroscope(app_name)
    tracing.py                    # OTEL setup + X-Trace-Id middleware helper
```

---

## Backlog

---

### OBS-0 — Docker Compose Infra ✅

All new services, volumes, configs. Zero breaking changes to existing services.

> **Implementation note:** monitoring services live in `infra/docker-compose.monitoring.yml` (separate from main `docker-compose.yml`), started via `make monitoring-up` / `make monitoring-up-all`. Both composes share `melo_default` network and `melo_melo_logs` volume. Config files generated by idempotent `infra/setup-infra.sh`.

#### New services in `infra/docker-compose.monitoring.yml`
* [x] `prometheus` — `prom/prometheus:latest`, port 9090
* [x] `loki` — `grafana/loki:latest`, port 3100
* [x] `promtail` — `grafana/promtail:latest`, scrapes shared log volume
* [x] `tempo` — `grafana/tempo:latest`, ports 4317/4318/3200
* [x] `grafana` — `grafana/grafana:latest`, port 3001, named volume for state
* [x] `pyroscope` — `grafana/pyroscope:latest`, port 4040
* [x] `celery-exporter` — `danihodovic/celery-exporter:latest`, port 9808
* [x] `flower` — `mher/flower:latest`, port 5555, no auth
* [x] `cadvisor` — `gcr.io/cadvisor/cadvisor:latest`, port 8090
* [x] `node-exporter` — `prom/node-exporter:latest`, port 9100
* [x] `postgres-exporter` — `prometheuscommunity/postgres-exporter:latest`, port 9187
* [x] `redis-exporter` — `oliver006/redis_exporter:latest`, port 9121
* [x] `admin` — custom Streamlit, port 8501 (OBS-6)

#### Volumes & config
* [x] Named volume `grafana_data`
* [x] Named volume `melo_melo_logs` — shared between `api`, `worker`, `promtail`
* [x] Named volumes for prometheus, loki, tempo, pyroscope data

#### `depends_on` health checks
* [x] `grafana` waits for `prometheus` (`service_healthy`); loki + tempo use `service_started`
* [x] `promtail` waits for `loki` (`service_started`)
* [x] Healthcheck probes defined for all infra services

#### Infra configs
* [x] `infra/prometheus/prometheus.yml` — all scrape targets including MinIO bearer token
* [x] `infra/loki/loki.yml`
* [x] `infra/promtail/promtail.yml`
* [x] `infra/tempo/tempo.yml`
* [x] `infra/pyroscope/pyroscope.yml`

#### Pending (apply before OBS-7)
* [ ] `CELERY_SEND_EVENTS=True` added to worker service env in `docker-compose.yml`
* [ ] `CELERY_TASK_TRACK_STARTED=True` added to worker service env in `docker-compose.yml`
* [ ] New env vars verified present in `example.env`: `GRAFANA_ADMIN_PASSWORD`, `TELEGRAM_BOT_TOKEN`, `TELEGRAM_CHAT_ID`, `ADMIN_PASSWORD`, `LOG_MAX_SIZE_MB`, `LOG_MAX_AGE_HOURS`, `LOG_BACKUP_BUCKET`, `LOG_RETENTION_DAYS`, `PYROSCOPE_SERVER_URL`

---

### OBS-1 — Structured Logging ✅

* [x] `app/core/log_events.py` — `LogEvent` enum, all members
* [x] `app/core/logging.py` — dual renderer (stdout ConsoleRenderer + file JSONRenderer)
* [x] `app/core/log_manager.py` — rotation + gzip + MinIO upload + APScheduler cleanup
* [x] Middleware health log suppression (200 = skip, non-200 = log)
* [x] Log coverage across all API routes, services, workers

---

### OBS-2 — Metrics ✅

* [x] `app/core/metrics.py` — all counters, gauges, histograms
* [x] `prometheus_fastapi_instrumentator` + `GET /metrics`
* [x] Call site instrumentation across songs, favorites, playlists, services

---

### OBS-3 — Distributed Tracing ✅

* [x] `app/core/tracing.py` — OTEL setup, auto-instrumentation, `extract_trace_id()`, `TraceIdMiddleware`
* [x] Manual spans in downloader, processor, storage
* [x] Celery `traceparent` context propagation
* [x] `trace_id` injected into every structlog record

---

### OBS-4 — Grafana Dashboards & Alerting ✅

* [x] Datasources provisioned: Prometheus, Loki, Tempo, Pyroscope
* [x] `api.json`, `pipeline.json`, `system.json` dashboards
* [x] `rules.yml` — 8 alert rules with `relativeTimeRange` on every data block (Grafana 13 requirement)
* [x] `telegram.yml` — contact point + notification policy

> **Fix applied:** Grafana 13 rejects `relativeTimeRange: {from: 0, to: 0}`. Every query node and `__expr__` node must have `relativeTimeRange: { from: 600, to: 0 }` (or 7200 for 2h window rules).

---

### OBS-5 — Continuous Profiling ✅

* [x] `pyroscope-io>=0.8.0` added to `pyproject.toml` dependencies
* [x] `app/core/profiling.py` — `configure_pyroscope(app_name)`, no-ops gracefully if URL unset
* [x] `app/main.py` — `configure_pyroscope("melo.api")` called in lifespan
* [x] `app/workers/celery_app.py` — `configure_pyroscope("melo.worker")` called in `on_worker_init`

---

### OBS-6 — Streamlit Admin Dashboard ✅

* [x] `admin/auth.py` — `is_authenticated()`, `login_page()` with `ADMIN_PASSWORD` env check
* [x] `admin/app.py` — login gate, sidebar nav, external tool links, page dispatch, logout
* [x] `admin/pages/overview.py` — health badges + Prometheus metric cards
* [x] `admin/pages/songs.py` — paginated table, status breakdown, re-queue for failed
* [x] `admin/pages/logs.py` — Loki tail, service + level filters
* [x] `admin/pages/metrics.py` — PromQL input + 5 presets
* [x] `admin/pages/alerts.py` — Grafana firing alerts + all rules
* [x] `admin/pages/db_health.py` — postgres-exporter + redis-exporter side by side
* [x] `admin/Dockerfile` — `FROM python:3.12-slim`
* [x] `admin/requirements.txt` — `streamlit>=1.35.0`, `requests>=2.32.0`
* [x] `admin` service added to `infra/docker-compose.monitoring.yml`
* [x] `make admin` target added to Makefile

---

### OBS-7 — Tests & Smoke ⬜ IN PROGRESS

Depends on: OBS-1 through OBS-6

#### Unit tests (`tests/unit/`)
* [x] `test_log_events.py` — `LogEvent` enum members
* [x] `test_logging.py` — `trace_id` present; JSONL valid; required fields
* [x] `test_log_manager.py` — roll on size; roll on age; `.gz`; MinIO upload path; cleanup
* [x] `test_tracing.py` — `extract_trace_id()` unknown; Celery `traceparent` headers
* [x] `test_metrics_unit.py` — histogram observation after download
* [x] `test_middleware_health.py` — health 200 suppressed; health non-200 logged
* [ ] `test_admin_auth.py` — NEW:
  * [ ] `is_authenticated()` returns `False` when session key absent
  * [ ] `login_page()` sets `authenticated=True` on correct password
  * [ ] `login_page()` leaves session unauthenticated on wrong password

#### Integration tests (`tests/integration/`)
* [x] `test_metrics_api.py` — `GET /metrics` 200 + `songs_submitted_total` present; counter increments
* [x] `test_tracing_api.py` — `X-Trace-Id` present; non-empty hex string

#### Smoke test additions (`tests/smoke_test.sh`)
* [ ] S25: `GET /metrics` returns 200
* [ ] S26: Response has `X-Trace-Id` header

#### Coverage
* [ ] Maintain ≥ 80% (was 94.77%)

---

## New Makefile Targets

```makefile
monitoring-up:      ## Start observability stack (main app must be running)
monitoring-up-all:  ## Start app + observability stack together
monitoring-down:    ## Stop observability stack
monitoring-restart: ## Restart observability stack
monitoring-status:  ## Show observability stack container status
grafana:            ## Open Grafana in browser
flower:             ## Open Flower in browser
admin:              ## Open Streamlit admin in browser
metrics:            ## Curl /metrics endpoint
alerts:             ## List active Grafana alerts via API
logs-loki:          ## Tail recent logs via Loki HTTP API
log-rotate:         ## Manually trigger log rotation in worker container
```

---

## New Ports

| Service           | URL                   |
| ----------------- | --------------------- |
| Grafana           | http://localhost:3001 |
| Prometheus        | http://localhost:9090 |
| Loki              | http://localhost:3100 |
| Tempo (OTLP gRPC) | http://localhost:4317 |
| Tempo (HTTP)      | http://localhost:4318 |
| Tempo (UI/API)    | http://localhost:3200 |
| Pyroscope         | http://localhost:4040 |
| Flower            | http://localhost:5555 |
| cAdvisor          | http://localhost:8090 |
| Node Exporter     | http://localhost:9100 |
| Postgres Exporter | http://localhost:9187 |
| Redis Exporter    | http://localhost:9121 |
| Streamlit admin   | http://localhost:8501 |

---

## Definition of Done

* [x] All 13 monitoring services start via `make monitoring-up-all`
* [ ] `GET /metrics` returns 200 and contains all custom metric names
* [ ] Every API response carries `X-Trace-Id` header
* [ ] Trace visible in Grafana Tempo with child spans from download, FFmpeg, MinIO
* [ ] Log lines in JSONL file have `trace_id` field matching Tempo trace
* [ ] Grafana dashboards load from provisioning — no manual import
* [ ] Telegram alert fires when a test rule triggers
* [ ] Pyroscope flame graph visible in Grafana system dashboard
* [ ] Streamlit admin: login works; all 6 pages render live data
* [ ] Log rotation: roll triggers on size, `.gz` appears in MinIO `melo-log-backups/`
* [ ] Smoke tests S25–S26 pass
* [ ] Coverage stays ≥ 80%
* [ ] `README.md` updated: new ports table, new `make` targets, observability section
* [ ] `CHANGELOG.md` entry for v0.4.0

---

## Decision Log

| Decision                                            | Reason                                                       |
| --------------------------------------------------- | ------------------------------------------------------------ |
| Grafana Alertmanager over standalone                | Built-in Grafana 10+; zero extra service; YAML-provisioned   |
| Promtail scrapes JSONL file (not Docker log driver) | Structured label extraction from JSON fields                 |
| Gzip + upload on roll (not batch)                   | Minimises local disk; no cron                                |
| APScheduler cleanup inside API process              | No extra container                                           |
| stdout stays ConsoleRenderer                        | Human-readable in `docker compose logs`                      |
| `trace_id` in every log line                        | Grafana log ↔ trace correlation                              |
| Pyroscope continuous, always-on                     | Catches intermittent spikes                                  |
| Streamlit password from env                         | Solo user; consistent with APP_ENV-driven config             |
| `X-Trace-Id` on every response                      | Browser-side debugging without Grafana                       |
| Single `metrics.py` module                          | Prevents duplicate Prometheus registration errors            |
| `relativeTimeRange` on every alert data node        | Grafana 13 requirement; `{from:0,to:0}` crashes provisioning |
| `configure_pyroscope` no-ops if URL unset           | Local dev unaffected; clean degradation                      |
| Monitoring in separate compose file                 | Main compose stays clean; monitoring is optional             |
| `setup-infra.sh` idempotent generator               | Safe to re-run; survives accidental deletion                 |
| loki/tempo `service_started` not `service_healthy`  | WSL2 ingester anti-flap; functionally ready sooner           |
| `pyroscope user: root`                              | Named volume owned by root on first create                   |
| `--wait` dropped from monitoring compose up         | loki/tempo/pyroscope cosmetically unhealthy on WSL2          |
