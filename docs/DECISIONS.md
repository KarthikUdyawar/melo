# Melo — Decisions

> Grouped by the sprint that made the call, using each sprint's own decision log as source of truth (`docs/sprints/Sprint-N.md`). Where the current-state `DECISIONS.md` had a decision not listed in any sprint log verbatim, it's placed under the sprint whose scope it matches and left unmarked; check the sprint doc if you need the exact original wording.

---

## Sprint 1 — Infrastructure & Ingest

| Decision                                                | Reason                                                                                                                                                     |
| ------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Dropped Alembic                                         | Solo project; `Base.metadata.create_all()` on startup is simpler and sufficient without schema drift risk                                                  |
| `APP_ENV`-driven env files                              | Clean separation: `.env.development` (localhost), `.env.staging` (Docker service names), `.env.production` (real infra)                                    |
| `reset_settings()` + `reset_db()`                       | Lets tests swap envs and DB state without process restart; no global state leaks between test cases                                                        |
| `expire_on_commit=False` on session                     | Avoids lazy-load errors after commit in async/Celery contexts                                                                                              |
| Pinned yt-dlp format selector (`140/251/…`)             | `bestaudio/best` requires a JS runtime for YouTube signature solving; explicit format IDs use plain HTTPS, no Node/Deno needed                             |
| `worker_ready` signal for MinIO bucket                  | Ensures bucket exists once per worker process at startup rather than checking on every task                                                                |
| `_BaseTask` with shared DB session                      | One SQLAlchemy session per worker process, closed in `after_return` — avoids per-task connection overhead                                                  |
| Proxy stream via FastAPI instead of presigned URL       | MinIO presigned URLs are signed against the internal Docker hostname (`minio:9000`); rewriting the host post-signing breaks HMAC. API proxies bytes itself |
| MinIO image tag pinned (`RELEASE.2024-05-01T01-11-10Z`) | Reproducibility; avoids silent breaking changes from `:latest`                                                                                             |
| `/tmp/melo` mounted as `tmpfs` in `api`/`worker`        | Avoids WSL2 Docker Desktop bind-mount permission/corruption issues                                                                                         |

---

## Sprint 2 — Processing, Metadata & API Polish

| Decision                                                                        | Reason                                                                                                                          |
| ------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------- |
| Dedup reuses existing MinIO object                                              | Same audio source, different trim — no re-download; new DB row allows different `start`/`end` per record while sharing one file |
| FFmpeg trim on stream, not on ingest                                            | Avoids storing N trimmed variants per song; one source file in MinIO, trim applied ephemerally per stream request               |
| `probe_metadata(download=False)` before download                                | Populates `title`/`thumbnail_url`/`duration` immediately so `GET /songs/{id}` is useful while `status == processing`            |
| Global exception handler wraps all errors in envelope                           | Single place controls error shape; routers never build error responses manually                                                 |
| Envelope skipped for stream / `/metrics` / `/docs` / `/redoc` / `/openapi.json` | Binary/non-JSON responses can't be wrapped; documented exceptions, not inconsistencies                                          |
| Structured JSON logs, pretty console in dev                                     | JSON parseable by aggregators in staging/prod; human-readable locally without config change                                     |
| Uvicorn access logs disabled                                                    | Middleware already logs method + path + status + duration — duplicate lines add noise                                           |
| Removed `unique=True` on `youtube_id`                                           | Dedup-with-trim needs multiple rows per video; uniqueness enforced at task level, not DB level                                  |
| `noplaylist: True` on both probe and download                                   | A `?v=X&list=Y` URL must resolve to the single video, not the playlist context                                                  |
| Pinned format selector on probe too                                             | `download=False` still triggers JS-runtime format checks; same explicit IDs suppress the hang                                   |
| All new `SongResponse` fields default to `None`                                 | Record is serialized immediately after insert, pre-probe; fields populated async by the worker                                  |
| Stream copy (`-c copy`) first, re-encode fallback                               | Stream copy is instant and lossless; re-encode (`libmp3lame -q:a 2`) only fires on codec mismatch                               |
| Cleanup in generator `finally` block                                            | Ensures `/tmp/melo` files deleted after the last byte sent, even on client disconnect                                           |

---

## Sprint 3 — Speed, Library Features & Metadata UX

| Decision                                                                              | Reason                                                                                              |
| ------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------- |
| UUID v7 for all PKs (`uuid6.uuid7`)                                                   | String-sortable = chronological = natural cursor pagination key                                     |
| Cursor pagination (`?after=<uuid>`) on `GET /songs`, `bookmark` = last id or `null`   | Stable under concurrent inserts; no offset drift. `offset` kept as a secondary convenience          |
| `count` = total matching records before pagination                                    | Lets clients show "N results" without an extra count query                                          |
| Preview endpoint (`POST /songs/preview`) is stateless                                 | No DB writes; simpler system; worker still re-probes on ingest as source of truth                   |
| Speed applied at stream time, not stored per-variant                                  | Avoids storing N speed variants of the same audio in MinIO                                          |
| Chained `atempo` filters for speed outside `[0.5, 2.0]`                               | FFmpeg caps a single `atempo` stage to that range                                                   |
| Trim always applied before speed                                                      | Reduces data before the (potentially lossy) re-encode step                                          |
| Favorites idempotent via check-then-insert + `IntegrityError` catch                   | Clean UX (200 vs 201) with DB-level dedup as a backstop                                             |
| `unique=True` on `favorites.song_id`                                                  | DB-level dedup regardless of app logic                                                              |
| `is_favorite` populated per song (N+1)                                                | Acceptable at MVP scale                                                                             |
| Playlist ordering via `position`, auto-incremented server-side                        | Predictable playback order; same song reusable across multiple playlists                            |
| `db.refresh(playlist)` after add / `db.expire_all()` after remove                     | Different SQLAlchemy identity-map staleness patterns for add vs. remove — both needed               |
| Soft delete (`deleted_at`) on `songs`, `favorites`, `playlists`                       | Audit trail on user-facing entities; no Alembic migration needed since it's just a model column     |
| `playlist_songs` hard-deleted                                                         | Join table has no audit need                                                                        |
| `_song_utils.py` shared `serialize_song()`                                            | Eliminates duplicate serialization logic; avoids circular imports                                   |
| `effective_duration` = `(end - start, or duration) / speed`, skip when `speed == 1.0` | Reflects actual playback duration after both trim and speed                                         |
| `docs_url`/`redoc_url` disabled in production                                         | Reduces attack surface                                                                              |
| Health check probes DB + Redis + MinIO                                                | Silent infra failure was previously undetectable                                                    |
| `DELETE /songs/{id}` also removes the MinIO object                                    | Was in the PRD but not implemented until this sprint                                                |
| GIN trigram index (`pg_trgm`) on `songs.title`                                        | Supports leading-and-trailing `ILIKE '%search%'`; extension created idempotently, skipped on SQLite |
| Node.js removed from Dockerfile                                                       | Pinned format selector is plain HTTPS; saved ~180MB and ~40s build time                             |
| `uv sync --frozen --no-install-project`                                               | Reproducible builds from lockfile; `melo` itself isn't installed as a package                       |
| `.dockerignore` populated (was empty)                                                 | Prevents `.git`, tests, coverage HTML, `.env*` from leaking into the build context                  |
| `make help` as default Makefile target                                                | Discoverability across 20+ (now 40+) targets                                                        |
| Root `conftest.py` sets env vars in `pytest_configure`                                | Ensures `APP_ENV=test` is set before any module-level settings are read                             |
| Unit test isolation via `_truncate_all()`, not savepoint rollback                     | Savepoint rollback is unreliable once endpoints call `db.commit()` directly                         |

---

## Sprint 4 — Vanilla JS UI

| Decision                                                                        | Reason                                                                                                                              |
| ------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------- |
| Vanilla HTML/JS/CSS over React/Vue                                              | Zero build step (~2s Docker build); avoids Node/pnpm/WSL2 memory pressure entirely — React/Vite was attempted first and scrapped    |
| ES modules (`type="module"`)                                                    | Clean imports without a bundler; native browser support                                                                             |
| Hash routing (`#/`)                                                             | No server-side routing config needed; nginx serves `index.html` for everything                                                      |
| Single `<audio>` element, module-scoped in `player.js`                          | Persistent playback across hash navigation without any framework state management                                                   |
| HTML strings + `innerHTML` rather than a virtual DOM                            | Simple and fast enough at this scale; no component framework needed                                                                 |
| `setInterval` for polling                                                       | Sufficient for a 2s poll; no reactive-query library needed                                                                          |
| nginx proxies `/api/*` → `api:8000`                                             | Single entry point; no CORS; `proxy_buffering off` required for audio streaming                                                     |
| No frontend tests                                                               | No framework = no component test surface; API already covered                                                                       |
| `ui/Dockerfile` is 3 lines                                                      | `FROM nginx:alpine` + `COPY` + done; ~2s build                                                                                      |
| Event delegation via one `document`-level click listener                        | Works for dynamically rendered cards without re-binding listeners per render                                                        |
| `httpx` proxy (no trim/speed) + `FileResponse` (trim/speed), chosen per-request | Only way to get real Range/206 support for the common case while still supporting FFmpeg-processed audio                            |
| Retry = delete old record + resubmit same params                                | Reuses the existing `POST /songs` path; original params read off the failed record; old record deleted only after resubmit succeeds |
| "New playlist" from the dropdown uses `window.prompt()`, not an inline field    | Simpler; no dedicated inline-input state needed                                                                                     |
| `hasMore = bookmark && records.length >= limit`                                 | A short page means end-of-results even with a non-null bookmark; prevents a ghost "Load more" button                                |
| No dedicated `renderModal()` — modal markup built inline in `app.js`            | Modals have divergent enough content that a shared template added little value                                                      |

---

## Sprint 5 — Observability & Monitoring

| Decision                                                                                | Reason                                                                                                                                                                                                        |
| --------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Grafana's built-in Alertmanager over a standalone service                               | Zero extra container; YAML-provisioned contact points; sufficient for a solo deployment                                                                                                                       |
| Promtail scrapes the JSONL log file, not the Docker log driver                          | Structured label extraction from JSON fields; decouples log format from transport                                                                                                                             |
| Gzip + upload to MinIO on log roll (not batched)                                        | Minimizes local disk use; no cron; simpler failure model                                                                                                                                                      |
| APScheduler cleanup runs inside the API process                                         | No extra container; survives app restarts as part of the normal lifecycle                                                                                                                                     |
| stdout stays `ConsoleRenderer`; file output is `JSONRenderer` (JSONL)                   | `docker compose logs` stays readable; Loki scrapes the file, not stdout                                                                                                                                       |
| `trace_id` injected into every log line                                                 | Enables Grafana log↔trace correlation without leaving the UI                                                                                                                                                  |
| `X-Trace-Id` header on every API response                                               | Browser-side debugging without opening Grafana                                                                                                                                                                |
| OTEL `traceparent` propagated into Celery task headers, reconstructed at task start     | HTTP request span and async task span appear in the same trace                                                                                                                                                |
| Pyroscope continuous profiling, always-on                                               | Catches intermittent CPU spikes; low overhead at this scale                                                                                                                                                   |
| `configure_pyroscope()` no-ops if server URL unset                                      | Local dev unaffected; clean degradation                                                                                                                                                                       |
| Single `app/core/metrics.py` module for all metric definitions                          | Prevents duplicate-registration errors; one place to look                                                                                                                                                     |
| `relativeTimeRange` explicit on every Grafana alert data node                           | Grafana 13 rejects `{from: 0, to: 0}`; every query/`__expr__` node needs an explicit window                                                                                                                   |
| Monitoring stack in a separate Compose file                                             | Main compose stays clean; monitoring is optional (`make monitoring-up` vs `make monitoring-up-all`)                                                                                                           |
| `loki`/`tempo`/`pyroscope` use `service_started`, not `service_healthy`                 | Their `/ready` endpoints are slow on WSL2 Docker Desktop (ingester anti-flap) even when functionally ready                                                                                                    |
| `pyroscope` container runs as `user: root`                                              | Its named volume is created root-owned on first run; default non-root image user can't write to it                                                                                                            |
| `--wait` dropped from the monitoring `compose up` invocation                            | loki/tempo/pyroscope report cosmetically unhealthy on WSL2; `--wait` would fail the whole command                                                                                                             |
| cAdvisor matches containers by cgroup ID, not name, on WSL2                             | Docker Desktop's daemon storage lives inside its internal VM; cAdvisor's docker factory can't resolve overlay2 layerdb even with `--privileged`; `make cadvisor-ids` re-run needed after container recreation |
| Health check log suppression only when `GET /health` is `200`                           | Grafana's ~10s datasource probes across every datasource would otherwise flood Loki; a non-200 health check is itself worth keeping                                                                           |
| Streamlit admin password from env, `hmac.compare_digest`                                | Solo user; simplest secure option, consistent with existing `APP_ENV`-driven config pattern                                                                                                                   |
| Streamlit admin pages query Prometheus/Loki/Grafana/Melo API directly, no iframe embeds | Keeps the admin app decoupled from Grafana's own auth/session model                                                                                                                                           |
| `melo_melo_logs` is a named volume, not a bind mount                                    | Bind mounts caused Promtail/Loki to receive zero logs; the named volume matches Promtail's `external: true` reference exactly                                                                                 |
| Grafana Telegram contact point shipped as a commented-out stub                          | Grafana 13 validates contact point credentials at boot even with placeholder env vars; real setup deferred to manual UI config once credentials exist                                                         |

---

## Hotfix — yt-dlp 403 / Test Env (Sep 2026, unsprinted)

| Decision                                                                                                       | Reason                                                                                                                                                                                                                                                                                                                                                                                               |
| -------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| yt-dlp bumped `>=2026.03.17` → `==2026.8.19`                                                                   | YouTube now defaults more clients through JS-signature solving (yt-dlp changelog, Feb 2026); worker log showed "No supported JavaScript runtime" warning followed by 403 on download                                                                                                                                                                                                                 |
| `pyproject.toml` deps switched from range (`>=`) to exact (`==`) pins                                          | Side effect of `uv lock` re-resolving the full dependency graph during the yt-dlp bump; kept deliberately since already locked and test-verified (434/434 passing) rather than reverted                                                                                                                                                                                                              |
| `.env.test` recreated                                                                                          | File had gone missing from the repo entirely; `dotenv_values()` silently returns `{}` on a missing file, so `force_env_file_priority` did nothing — `DATABASE_URL` set by `tests/unit/conftest.py` (SQLite) leaked into integration tests during combined `make test` runs, causing "no such table" / "detached connection fairy" failures. Not a regression from the dep bump — coincidental timing |
| `.env.test` MinIO bucket set to `songs-test`                                                                   | Keeps test bucket name distinct from dev's `songs`, cheap safety even though storage is tmpfs/ephemeral in `docker-compose.test.yml`                                                                                                                                                                                                                                                                 |
| `PYROSCOPE_SERVER_URL` left blank in `.env.test`                                                               | `configure_pyroscope()` no-ops on unset per existing Sprint 5 decision — removes noisy pyroscope lines from test output                                                                                                                                                                                                                                                                              |
| `minio/minio` Docker Hub image migrated to `quay.io/minio/minio:RELEASE.2025-09-07T16-13-09Z.hotfix.7aa24e772` | MinIO pulled all images from Docker Hub (Oct 2025 security-CVE dispute) and archived the community repo as unmaintained (Feb 2026); `minio/minio` pulls now fail with "repository does not exist" even for previously-working pinned tags. Verified this quay.io tag pulls clean and passes the full integration suite (193/193)                                                                     |

**Still open:** whether the yt-dlp version bump alone resolves the 403, or whether re-adding a JS runtime (`deno`) to the worker image is required — pending a real-URL retest against rebuilt containers. If deno is needed, that decision reverses Sprint 3's "Node.js removed from Dockerfile" entry and needs its own log line here once confirmed.

---

## Sprint 6 — Frontend Polish

| Decision                                                                                                                                              | Reason                                                                                                                                                                                                                                                                                                                                   |
| ----------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Mobile support added, overturning Sprint 4's "desktop-only" call                                                                                      | Karthik's call this sprint — personal use now includes phone                                                                                                                                                                                                                                                                             |
| Breakpoints: desktop >=1280px (unchanged), tablet 768-1279px, phone <=767px                                                                           | Standard tablet/phone split; existing desktop CSS becomes the top tier, mobile-first below it                                                                                                                                                                                                                                            |
| Tablet nav = icon rail (56px), no drawer/hamburger                                                                                                    | Spotify-style; Karthik's call — simpler than a toggled drawer, zero extra JS state                                                                                                                                                                                                                                                       |
| Phone nav = bottom tab bar (Library/Favorites/Playlists)                                                                                              | Matches common mobile music-app patterns                                                                                                                                                                                                                                                                                                 |
| Phone-only floating Add Song button (FAB)                                                                                                             | Sidebar (which houses Add Song on tablet/desktop) is hidden entirely on phone, and Add isn't one of the 3 tab-bar routes — FAB is the only remaining path                                                                                                                                                                                |
| Add-song trigger uses a shared `.btn-add-song-trigger` class, not a single id                                                                         | Two DOM buttons (sidebar + FAB) now open the same modal; querySelectorAll binding avoids an id collision                                                                                                                                                                                                                                 |
| Song card stacks (thumb top, text below) on phone                                                                                                     | Literal reading of PRD wording; not yet visually confirmed on a real device — flagged in `TODO.md` FE-5 for audit                                                                                                                                                                                                                        |
| Modal full-screen only below 768px, centered card unchanged at 768px+                                                                                 | Matches PRD; reuses the existing `modal-in` animation at both tiers                                                                                                                                                                                                                                                                      |
| Player queue built from `state.currentSongList` (page's rendered list), not refetched per click                                                       | Card click already has the sibling songs on screen; avoids an extra `GET /songs/{id}` per play and gives prev/next/shuffle a queue for free                                                                                                                                                                                              |
| Shuffle re-finds current song's index after Fisher–Yates reshuffle                                                                                    | Keeps the currently-playing song playing uninterrupted; PRD requirement, not a full in-place partial shuffle                                                                                                                                                                                                                             |
| `next`/`prev` skip queue entries where `status !== 'done'`                                                                                            | Non-done songs can be queue-adjacent (pending/processing next to it in the list) and aren't streamable yet                                                                                                                                                                                                                               |
| `prev()` restarts current song if >3s played, else jumps to previous track                                                                            | Standard media-player convention; not explicitly specified in PRD — flagged in `TODO.md` FE-5 for Karthik to confirm/override                                                                                                                                                                                                            |
| Loop-mode single 3-state button (`data-mode` attribute) instead of 3 separate buttons                                                                 | Matches DESIGN.md's existing icon-button pattern (reuses one slot); badge overlay shows "1" only in loop-one state                                                                                                                                                                                                                       |
| `bindScrubber` folded into `bindPlayerControls`, single call site in `app.js`                                                                         | FE-1 added 5 more controls needing the same "bind once after DOM ready" treatment; one export is simpler than five                                                                                                                                                                                                                       |
| Removed the old top-level `document.getElementById(...).addEventListener(...)` in `player.js`                                                         | Was a side effect at module-import time outside any function — moved into `bindPlayerControls()` for clean-code/no-hidden-side-effects-on-import                                                                                                                                                                                         |
| Reorder uses a temp sentinel position (-1), not the add-song retry-on-IntegrityError pattern                                                          | _reposition_song avoids the unique-position conflict and its 409 retry; row locking still serializes concurrent reorders, so `409` (which existed for `add_song_to_playlist`'s optimistic-insert race) doesn't apply here. Confirmed via TDD: no test could naturally produce it.                                                        |
| Reorder validates the upper-bound position (`>= song_count`) in the route handler, not the Pydantic schema                                            | Schema-level `Field(ge=0)` only knows the request body; the upper bound depends on runtime `song_count`, which requires a DB query                                                                                                                                                                                                       |
| Drop-onto-row inserts dragged song *after* the target row's current index                                                                             | Simplest mapping from native HTML5 DnD `drop` target to the existing 0-indexed `position` field; not explicitly specified in PRD. Karthik manually tested and confirmed working — kept as-is, no change requested.                                                                                                                       |
| Drag state (`dragSongId`, `currentPlaylistId`) kept in module-scope `state`, not `event.dataTransfer`                                                 | `dataTransfer.getData()` isn't reliably readable during `dragover`/`dragleave` in all browsers; plain JS state avoids that gotcha, consistent with existing player-queue pattern                                                                                                                                                         |
| Thumbnail drag-preview shows only the image, not the full row (cosmetic)                                                                              | Browsers make `<img>` natively draggable by default; reorder logic still works via `closest()` bubbling to the row, so functionally correct — fix deferred to Sprint 7, flagged in `TODO.md`, not fixed this sprint to avoid touching shared `renderSongCard` mid-sprint                                                                 |
| `smoke_test.sh` S17 builds 3 real songs sharing one `youtube_id` with different `start`/`end`                                                         | Exercises dedup logic (MODELS.md: non-unique `youtube_id`) deliberately so reorder has 3 real playlist rows to shift, not synthetic/mocked data                                                                                                                                                                                          |
| `smoke_test.sh` S8 fails fast with an explicit dangling-`file_url` diagnosis on a 502, rather than a bare error                                       | A real failure mode was hit during Sprint 6 smoke testing: DB/MinIO volume drift (MinIO wiped without a matching Postgres reset) leaves a deduped `Song.file_url` pointing at a deleted object. Diagnosed via API logs (upstream 404 from MinIO on the presigned GET); not an app bug, but worth surfacing clearly instead of a bare 502 |
| Now Playing panel state synced via a `subscribe()` pub-sub in `player.js`, not polled/duplicated                                                      | Panel and player bar must show identical live state (play/pause, scrubber, volume, shuffle, loop, current song) without the panel owning its own timers; `emit()` fires from the same 5 UI-update functions the bar already uses, panel just adds itself as a listener                                                                   |
| Waveform peaks cached in `player.js` module scope (`peaksCache`), not `app.js`'s `state` object                                                       | Cache must survive hash navigation and panel close/reopen the same way `currentSong` does; keeping it next to the audio element avoids a second source of truth for "what have we already decoded"                                                                                                                                       |
| Panel scrubber supports click-to-seek; waveform canvas does not                                                                                       | Two different controls — PRD's "no click-to-seek this sprint" scoped the *waveform*, not the *scrubber* (a scrubber with no seek is a bug, not a feature). Confirmed with Karthik after he flagged it as broken in manual testing                                                                                                        |
| Loop-badge CSS switched from `#btn-loop[data-mode="one"]` to a shared `.loop-btn[data-mode="one"]` class                                              | ID-scoped selector only matched the player bar's loop button; the panel's `#np-loop` button never got the badge. Both buttons now carry `.loop-btn` so one rule covers both surfaces — same fix pattern will apply to any future third loop button                                                                                       |
| Panel scrubber drag-state (`state.npSeeking`) kept separate from player bar's internal `isSeeking`                                                    | Two independent scrubbers can't share one seeking flag — dragging the panel's must not be interrupted by the bar's `timeupdate` handler and vice versa; mirrors the existing bar pattern rather than reusing it                                                                                                                          |
| Esc priority chain is panel → dropdown → modal, not a flat close-everything                                                                           | Each surface needs "only close the topmost thing" semantics; a flat handler would close the modal while a dropdown inside it was still open, or vice versa                                                                                                                                                                               |
| Now Playing panel gets the same manual focus trap as `.modal`, not a separate mechanism                                                               | Panel is a second full-screen surface with its own focusable set (close button, transport, 2 sliders) — same UX gap as modals, wasn't listed in the original FE-4 ticket since it was written before FE-3 existed. `trapFocus()` takes the container as a param so both reuse one function.                                              |
| Dropdown `aria-expanded` synced in three places (toggle click, outside-click handler, `closeOpenDropdown()`) rather than one central setter           | No existing state store for "which dropdown is open" — menus are toggled via inline `style.display` per Sprint 4's original design; matching that pattern in three call sites was simpler than introducing a new state field for one attribute                                                                                           |
| Playlist drag-reorder rows found to have no keyboard alternative during FE-4 audit — fixed in the same ticket (Karthik's call)                        | Native HTML5 DnD (FE-2, Sprint 6) has no built-in keyboard path; discovered auditing tab order, out of original FE-4's initial scope, but Karthik chose to fix immediately rather than defer — see the keyboard-reorder row below for the implementation                                                                                 |
| Keyboard reorder reuses `reorderPlaylistSongOptimistic()`, no separate keyboard-only code path                                                        | Drag and Arrow-key reorder are just two input methods calling the same optimistic-update + PATCH + resync-on-failure logic; avoids duplicating the "insert after target" semantics FE-2 already established                                                                                                                              |
| Row kept as plain focusable `<div>`, not given `role="button"` or a listbox/option ARIA pattern                                                       | Row wraps other interactive elements (song card, remove button) — an interactive role on the wrapper would create nested-interactive ambiguity for screen readers; arrow keys don't conflict with anything else a focused div does, so no formal ARIA widget pattern was needed for this scope                                           |
| Scrubber/volume progress fill driven by a `--progress` CSS custom property, set in JS, not CSS alone                                                  | `<input type="range">` has no CSS-only way to read its own live `.value` for a fill gradient — must be pushed from JS on every `input`/`timeupdate` tick, on both Player Bar and Now Playing panel controls                                                                                                                              |
| Waveform played/unplayed coloring redraws the full canvas every `timeupdate` tick, not a clip-path/overlay                                            | Simpler than a two-layer canvas or clip-path approach; redraw cost is negligible at 200 peak buckets                                                                                                                                                                                                                                     |
| Now Playing panel scrubber seek unified into one `commitSeek()` guarded by `npSeeking`, fired from `change` or `mouseup`/`touchend`                   | Separate handlers could race — `change` didn't always fire reliably in testing, leaving the seek silently uncommitted and playback resuming from the stale position                                                                                                                                                                      |
| Custom favicon shipped as a PNG asset (`ui/assets/logo.png`), not an inline SVG data-URI                                                              | Karthik wanted a specific AI-generated mark; a PNG asset was simpler to iterate on than hand-authoring/re-encoding SVG each revision                                                                                                                                                                                                     |
| Player Bar empty state uses a dedicated placeholder element toggled opposite `.player-bar--empty`, not a `display` swap on the existing info/controls | Existing info/controls use `visibility:hidden` (preserves bar height so the layout doesn't jump); placeholder needed the inverse toggle to occupy the same box without fighting that                                                                                                                                                     |

## Post-Sprint 6 — CodeRabbit Review Fixes

| Decision                                                                                                                                                                                                                                                                       | Reason                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Playlist position compaction runs as `db.delete(entry)` → `db.flush()` → `_compact_positions()` → `db.commit()`, delete-then-compact in that exact order within one transaction                                                                                                | First implementation attempt ran compaction before the delete (no-op, since the row wasn't gone yet) — caught by the regression test failing `[0, 2]` instead of `[0, 1]`. Order matters; not obvious from a diff alone.                                                                                                                                                                                                                                                   |
| Shuffle keeps a separate `originalQueue` array rather than re-deriving order some other way                                                                                                                                                                                    | Simplest correct fix — mirrors the existing `shuffleQueueKeepingCurrent()` pattern (find current song by id, keep it in place) for the reverse direction too                                                                                                                                                                                                                                                                                                               |
| `playSongById` id-miss falls back to a solo `player.loadSong()` fetch, not a silent no-op or a queue rebuild                                                                                                                                                                   | The id isn't in any known list at that point, so there's no queue to build; fetching and playing standalone is the smallest correct recovery, consistent with `handleRetrySong`'s existing `api.getSong` pattern                                                                                                                                                                                                                                                           |
| Now Playing panel's document-level scrubber-commit listener is tracked in `state.npCommitSeekHandler` and explicitly removed on close                                                                                                                                          | The listener has to live on `document` (release can happen outside the scrubber), but that means it isn't naturally scoped to the panel's own DOM lifecycle like everything else — needed manual cleanup                                                                                                                                                                                                                                                                   |
| `state.npPeaks` cleared on panel close, not only on song change                                                                                                                                                                                                                | `player.subscribe()`'s initial synchronous callback fires before the new song's `loadAndDrawWaveform()` can null it out — clearing on close (rather than reordering the open-sequence calls) was the smaller, more localized fix                                                                                                                                                                                                                                           |
| Reorder endpoint locks all `PlaylistSong` rows for the playlist (`SELECT ... FOR UPDATE`) before reading membership/count, in the same transaction as the shift                                                                                                                | `API_DOC.md` claimed "no concurrent-write race window" but no lock was actually taken — two concurrent reorders on the same playlist could interleave outside the sentinel-swap's own transaction. FOR UPDATE serializes them on Postgres; SQLite ignores row locks (no unit-test coverage for this path).                                                                                                                                                                 |
| Song dropdown menu markup dropped `role="menu"`/`role="menuitem"`, kept plain `<button>`s                                                                                                                                                                                      | ARIA menu roles imply arrow-key navigation and roving focus, which the widget never implemented (Tab reaches each button individually, click delegation handles activation, Escape already closed it independent of role). A role promising keyboard behavior that isn't there is worse than no role — native button semantics are accurate and need no new JS. `aria-haspopup`/`aria-expanded` on the trigger kept, since those don't require `role="menu"` on the popup. |
| `app/api/playlists.py`: `_reposition_song()` no longer calls `db.commit()` itself — commits moved to the route handler right after the call                                                                                                                                    | Matches the existing `DELETE .../songs/{song_id}` transaction boundary (delete→flush→compact→commit); helper functions shouldn't own the transaction, callers should                                                                                                                                                                                                                                                                                                       |
| `app/api/playlists.py`: dead `_get_membership_or_404()` helper removed                                                                                                                                                                                                         | Never called — both `reorder_song_in_playlist` and `remove_song_from_playlist` already scan the `_lock_playlist_songs()` result (post-lock list), not a fresh query. Unused code removed rather than wired in, since wiring it in would mean a second redundant query against already-locked rows                                                                                                                                                                          |
| Reorder now logs via `LogEvent.PLAYLIST_SONG_REORDERED` (added to `log_events.py`) and increments `playlist_ops_total.labels(action="reorder")`                                                                                                                                | Matches the add/remove handlers' existing conventions — raw string event names and missing metrics were an oversight from when FE-2's endpoint first shipped                                                                                                                                                                                                                                                                                                               |
| `ui/player.js`: `setVolume()` debounces the `localStorage` write (300ms after last call), audio.volume + UI update stay synchronous                                                                                                                                            | Slider `input` fires on every drag tick; writing to disk on each one was wasteful. Immediate audio/UI feedback preserved, only persistence is delayed                                                                                                                                                                                                                                                                                                                      |
| `ui/player.js`: `getPeaks(songId, signal)` takes an optional `AbortSignal`, passed to the waveform fetch; `ui/app.js`'s `loadAndDrawWaveform()` owns an `AbortController` per panel-open/song-switch, aborting the previous one before starting a new fetch and on panel close | Panel close or a fast next/prev while the panel is open previously left the old fetch+decode running to completion for nothing. Abort errors fall through the existing catch-and-cache-clear path, so retry-on-reopen still works                                                                                                                                                                                                                                          |
| `ui/components.js`: stray `role="menuitem"` left on the Delete dropdown item was removed                                                                                                                                                                                       | Leftover from before the dropdown-role rollback above — the intent was zero ARIA menu roles on any item, Delete was just missed. Now consistent with the rest of the menu                                                                                                                                                                                                                                                                                                  |
| `docs/ARCHITECTURE.md` / repo file `docs/PRROJECT.tree` → `docs/PROJECT.tree`; `Makefile`'s `tree` target output path fixed to match                                                                                                                                           | `ARCHITECTURE.md`'s link already said `PROJECT.tree` correctly — the actual generated file and the Makefile target had the typo, not the doc. `README.md` was already correct, no change there                                                                                                                                                                                                                                                                             |
| `ui/app.js`: `reorderPlaylistSongOptimistic()` comment rewritten, no code change                                                                                                                                                                                               | Old comment didn't describe direction-dependent placement (after target moving down, before target moving up) — reviewer flagged the doc/behavior gap, not a bug                                                                                                                                                                                                                                                                                                           |
| `README.md`'s "Out of Scope (v1)" list dropped `waveforms` from the "never" row                                                                                                                                                                                                | Stale since Sprint 6 shipped waveform display; only click-to-seek is still deferred (already tracked separately under Sprint 7+ candidates) — leaving "waveforms" in the never-list contradicted the shipped feature                                                                                                                                                                                                                                                       |
| `tests/smoke_test.sh` add-to-playlist calls now check status + call the fail handler                                                                                                                                                                                           | Matches the existing song-creation check pattern; an add-song failure here was previously silent until a much later, harder-to-diagnose ordering assertion failed                                                                                                                                                                                                                                                                                                          |
| `tests/smoke_test.sh` nil-UUID PATCH labels: "unknown song" → "unknown playlist"                                                                                                                                                                                               | The check exercises a nonexistent *playlist* segment, not song — label was just wrong, behavior (404) unchanged                                                                                                                                                                                                                                                                                                                                                            |
| `tests/smoke_ui.sh` jq-missing skip message lists every check it's skipping                                                                                                                                                                                                    | Previously said something generic; a dev without `jq` installed had no idea what coverage they were losing (create/get/cleanup/add-unknown-song/remove-unknown-song/2× FE-2 reorder checks)                                                                                                                                                                                                                                                                                |
| Now Playing panel gets `role="dialog"`/`aria-modal="true"`, a reopen-guard, and explicit focus-in/focus-out management (`#player-info-trigger` on close)                                                                                                                       | Panel already had a manual focus trap but nothing marked it as a dialog to assistive tech, nothing moved focus into it on open, and nothing restored focus on close — trap alone doesn't cover entry/exit focus semantics                                                                                                                                                                                                                                                  |
| `drawWaveform()` clamps `barWidth` so it can't go negative when peak count exceeds canvas width                                                                                                                                                                                | Narrow canvases (e.g. phone) with the full ~200-bucket peak array could produce a negative bar width, silently breaking the render instead of just looking dense                                                                                                                                                                                                                                                                                                           |
| `.player-volume` sets `display:flex; flex-direction:row` in its base rule, no longer split across a media query                                                                                                                                                                | The Now Playing panel's volume control and the Player Bar's shared this class but needed different display context at different breakpoints under the old rule; consolidating removes a source of the "one surface renders, one doesn't" class of bug already seen once this sprint (see FE-5 icon-alignment fix above)                                                                                                                                                    |

## Sprint 7 — Frontend Rewrite (Next.js/TS/Tailwind)

| Decision                                                                                                                                                                                                                   | Reason                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Vanilla JS/CSS/nginx → Next.js 14 (App Router) + React + TypeScript, reversing Sprint 4's "no framework, no build step" call                                                                                               | Component/state complexity (queue, waveform, subscribe pub-sub, dropdown aria sync) outgrew hand-rolled DOM strings — see Sprint 6 FE-3/FE-4 and the Post-Sprint-6 CodeRabbit section for the exact bug class this produced (stale document-level listeners, ID-scoped CSS missing a second surface, `change`/`mouseup`/`touchend` races)                                                                                                                                                                                                                           |
| Real routes (`/playlists/[id]`), not hash routing                                                                                                                                                                          | Karthik's call — idiomatic Next, cleaner URLs than `#/playlists/:id`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| Static export (`output: 'export'`) kept over `next start`                                                                                                                                                                  | No new Node runtime container in prod; nginx keeps serving static files, same topology as Sprint 4–6's `INFRA.md`                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| Dynamic playlist route resolved client-side via a placeholder `generateStaticParams` shell + nginx rewrite                                                                                                                 | Static export requires all dynamic segments known at build time; playlist IDs are runtime DB data. Shell page at `/playlists/_/` is emitted at build time; nginx rewrites any real `/playlists/<uuid>/` request to that shell; the page reads the actual ID from `window.location.pathname` client-side. Address bar shows the real URL throughout                                                                                                                                                                                                                  |
| Tailwind for utility styling, small hand-written `globals.css` kept for range-input thumb pseudo-elements, the `--progress` gradient-fill var, and `@keyframes`                                                            | These three have no clean Tailwind-utility equivalent; forcing them into arbitrary-value utilities would be less readable than the existing hand-written rules, which port over unchanged                                                                                                                                                                                                                                                                                                                                                                           |
| pnpm over npm/yarn                                                                                                                                                                                                         | Faster installs, strict `node_modules` layout — catches phantom-dependency bugs the old zero-dep setup never had to worry about                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| msw over manual `jest.fn()` fetch mocks, reversing the ponytail-default lean-mocking preference                                                                                                                            | Karthik's call — request-level mocking is closer to real fetch behavior; accepted as one extra devDep given `lib/api.ts` is the single most test-critical module in the new stack                                                                                                                                                                                                                                                                                                                                                                                   |
| Jest + React Testing Library introduced, reversing Sprint 4/6's "no frontend tests" call                                                                                                                                   | A real component framework now exists, so a real test surface exists too — this was the stated reason FE tests were skipped before (see Sprint 4/`PRD.md` FE-6)                                                                                                                                                                                                                                                                                                                                                                                                     |
| `AudioContext`/`<canvas>` waveform decode and native HTML5 `dataTransfer` get logic-only unit tests, not full jsdom DOM tests                                                                                              | Both are unsupported/flaky in jsdom; peak-downsampling math and reorder-index math are pure functions, tested directly instead of via simulated DOM events that jsdom can't faithfully reproduce                                                                                                                                                                                                                                                                                                                                                                    |
| `PlayerProvider` mounted once in root `layout.tsx`                                                                                                                                                                         | Root layout persists across Next App Router navigation — same "audio element survives page change" guarantee the vanilla `player.js` module-singleton had                                                                                                                                                                                                                                                                                                                                                                                                           |
| HTTPS via self-signed cert baked into the `ui` nginx image, not Let's Encrypt/certbot or a separate reverse-proxy container                                                                                                | Solo self-hosted box on a home network, no public domain to validate against; certbot's renewal automation doesn't apply here, and a dedicated reverse-proxy container was considered and rejected as unneeded extra infra for this scope                                                                                                                                                                                                                                                                                                                           |
| `lib/types.ts` uses plain TypeScript interfaces, no runtime validation library (zod/io-ts)                                                                                                                                 | The backend is the same trusted, solo-authored FastAPI service `API_DOC.md` already documents exactly — adding schema validation duplicates a contract that's enforced server-side already (Pydantic) and never crosses a real trust boundary here                                                                                                                                                                                                                                                                                                                  |
| `ApiError` is a real `class extends Error` (not a plain `{message, status}` object)                                                                                                                                        | Lets `instanceof ApiError` checks work in calling code and keeps `.message`/stack traces behaving like normal JS errors; ported the shape of the original `apiFetch()`'s `error.status = res.status` pattern into a proper type instead of a mutated plain `Error`                                                                                                                                                                                                                                                                                                  |
| `apiFetch<T>()` kept as a single generic helper in `api.ts`, not split per-resource                                                                                                                                        | Direct port of `ui/api.js`'s `apiFetch()` — one envelope-unwrap/error-shape code path for every endpoint, same as before; no reason to fragment it now that types replace the JSDoc comments                                                                                                                                                                                                                                                                                                                                                                        |
| `ui` host ports changed from the planned `80:80`/`443:443` to `4000:80`/`4443:443`                                                                                                                                         | Karthik's LAN machine already runs other Docker services on standard ports 80/443/3000. Container-internal nginx still listens on 80 (redirect)/443 (TLS) unchanged — only the host-side mapping moved. `nginx.conf`'s HTTP→HTTPS redirect updated to `return 301 https://$host:4443$request_uri` since the internal 80→443 redirect has no way to know the host remapped 443 to 4443.                                                                                                                                                                              |
| Dropped `.eslintrc.json`'s `@typescript-eslint/no-unused-vars` rule rather than installing the plugin                                                                                                                      | Rule referenced a plugin (`@typescript-eslint/eslint-plugin`) never added as a devDep, breaking `next build`'s lint step. `next/core-web-vitals` already covers unused-vars; not worth the extra dependency for a rule that's already redundant (ponytail: don't add what you don't need).                                                                                                                                                                                                                                                                          |
| `postcss.config.js` renamed to `postcss.config.cjs`, no content change                                                                                                                                                     | `package.json`'s `"type": "module"` makes Node parse any bare `.js` file as ESM; postcss's CommonJS `module.exports` syntax crashed under that. `.cjs` extension forces CommonJS parsing regardless of the package-level `type` field — smallest possible fix.                                                                                                                                                                                                                                                                                                      |
| `jest.config.ts`'s dead `setupFilesAfterEach: []` key removed                                                                                                                                                              | Typo for `setupFilesAfterEnv` (which was already present, correctly, on the next line) — not a real Jest config option, failed TS type-checking during `next build`'s lint/typecheck pass.                                                                                                                                                                                                                                                                                                                                                                          |
| Placeholder `src/app/page.tsx` added ahead of FE7-2/FE7-5                                                                                                                                                                  | Static export had nothing to emit for `/` (no page component existed yet), so the old stock nginx welcome page was never actually overwritten — `COPY` in the Dockerfile only adds files, doesn't clear the target dir first. Placeholder unblocks verifying the full Docker/nginx pipeline works before real page content exists. **Must be replaced by FE7-2/FE7-5, not shipped as-is.**                                                                                                                                                                          |
| `msw-polyfills.ts` rewritten to use `require()` instead of `import`, populate the full fetch-primitive set (`fetch`/`Headers`/`Request`/`Response`/`ReadableStream`/`TransformStream`/`WritableStream`/`BroadcastChannel`) | The file's original comment claimed undici polyfills were wired in, but no import/assign actually existed — first real `pnpm test` run failed immediately. ES `import` statements hoist above all other statements regardless of source position, so a naive `import`-based polyfill still runs *after* undici's own module-init reads `global.TextEncoder`/`ReadableStream` — `require()` gives real execution order control. `BroadcastChannel` (unused in our tests, but imported internally by msw's WebSocket support) polyfilled via Node's `worker_threads`. |
| `jest.config.ts`'s `transformIgnorePatterns` applied to the post-`createJestConfig()`-merge result, not passed alongside the pre-merge `config` object                                                                     | `next/jest`'s wrapper prepends its own blanket `/node_modules/` ignore pattern and Jest OR-matches the array — any custom exception passed as a sibling key to `next/jest`'s `config` argument gets silently defeated by that prepended pattern. Confirmed via `pnpm jest --showConfig`. Fix: wrap `createJestConfig(config)` in an async function and overwrite `transformIgnorePatterns` on its return value instead.                                                                                                                                             |
| `testEnvironmentOptions: { customExportConditions: [""] }` added to `jest.config.ts`                                                                                                                                       | Jest's jsdom test environment doesn't select the right `package.json` `exports` condition for msw@2's conditional-exports setup by default, so `msw/node` was unresolvable even though it exists on disk. Documented upstream msw@2+Jest interaction, not project-specific.                                                                                                                                                                                                                                                                                         |

**Still open:** whether static export's client-resolved-dynamic-route workaround is acceptable long-term, or whether a future sprint should eat the Node-runtime-container cost of `next start` for genuine SSR on `/playlists/[id]`. Revisit if the workaround causes real bugs (e.g. a stale shell served by a CDN/cache layer, though none exists in this stack today).
**Note:** none of the six `pnpm test` fixes above were guessable from reading the config files alone — each only surfaced by actually running `pnpm install && pnpm test`, confirming `handoff.md`'s prior "nothing in this sprint has actually been run yet" flag was the right call to prioritize. `FRONTEND_SETUP.md`'s Common Gotchas table should get entries for these; not done this session.
**Also still open:** `docker-compose.yml`/`INFRA.md` port-mapping update for the `ui` service (3000:80 → 80:80/443:443) hasn't been applied yet — flagged in `TODO.md`, needs a diff next session.

## Sprint 7 — FE7-2/FE7-5, this session

| Decision                                                                                                                                                  | Reason                                                                                                                                                                                                                                                                                                                                                                  |
| --------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `<ConfirmDialog>` built under FE7-2, not deferred to FE7-8                                                                                                | Re-reading PRD's FE7-2 component table: it lists `<ConfirmDialog>` explicitly alongside `<AddSongModal>` in the same "inline modal builders" row — only `<AddSongModal>`'s 2-step flow is FE7-8 scope. Library/Playlists pages (FE7-5) need delete-confirm now, so building it here rather than blocking on FE7-8.                                                      |
| `trapFocus()` drops the `el.offsetParent !== null` visibility filter from the original vanilla port                                                       | jsdom has no layout engine — `offsetParent` is always `null` there regardless of real visibility, which silently emptied the focusables list and made the trap a no-op in tests. Real hidden-element filtering, if ever needed, should check `.hidden`/computed `display`, not `offsetParent`, which only reflects layout.                                              |
| `SongCard`'s thumbnail ported to `next/image`'s `<Image unoptimized>` instead of a plain `<img>`                                                          | Silences the `@next/next/no-img-element` lint warning; `unoptimized` kept explicit on the element (not just relying on the global `next.config.mjs` setting) so the component stays correct if ever reused outside this static-export build. `Nav.tsx`'s sidebar logo `<img>` left as plain `<img>` for now — same tradeoff, not yet ported, low priority.              |
| `tsconfig.json` excludes `src/test/**/*`                                                                                                                  | `next build`'s production typecheck was pulling Jest-only setup files (`msw-polyfills.ts`) into scope via the blanket `**/*.ts` include, where their `require()`-based global polyfills collided with `lib.dom`'s declarations. Jest itself doesn't consult this include/exclude list (uses `next/jest`'s own transform), so excluding here doesn't affect `pnpm test`. |
| Stray `/* eslint-disable @typescript-eslint/no-var-requires */` removed from `msw-polyfills.ts`                                                           | The referenced rule was never registered — `next/core-web-vitals` doesn't pull in the `@typescript-eslint` plugin at all, same root cause as Sprint 7's earlier `no-unused-vars` rule drop. An unregistered-rule disable comment is itself a lint error, not a suppression.                                                                                             |
| `.eslintignore` added for `out/`, `.next/`, `node_modules/`                                                                                               | `pnpm lint` was walking into the static-export build output (`out/`) and flagging React internals inside minified vendor bundles — build artifacts, not source, were never meant to be linted.                                                                                                                                                                          |
| Nav rendering (sidebar + tab bar + FAB) built as one `<Nav>` client component using `usePathname()`, replacing the old hash-router's `highlightNavLink()` | Real routes (this sprint's core reversal) need real active-link detection; `usePathname()` is the direct Next equivalent of the old `window.location.hash` check.                                                                                                                                                                                                       |
| Add Song trigger buttons in `<Nav>` (sidebar + phone FAB) render as disabled/stub buttons this session, not wired to any modal                            | `<AddSongModal>` doesn't exist yet — that's FE7-8. Rendering the buttons non-functional (rather than omitting them) keeps the page layout/breakpoint behavior matching `DESIGN.md` ahead of FE7-8 landing.                                                                                                                                                              |
| Player bar in `layout.tsx` is a static empty-state placeholder, not wired to any audio state                                                              | `PlayerProvider` doesn't exist yet — that's FE7-4. Placeholder markup keeps the CSS grid's 3 rows (main/player-bar/tab-bar) from collapsing before the real player lands, matching the old vanilla `.player-bar--empty` behavior.                                                                                                                                       |

**Still open, blocking `pnpm build`:** `/playlists/[id]/page.tsx` combines `"use client"` with an exported `generateStaticParams()` — Next 14 disallows this combination outright (`generateStaticParams` must live in a Server Component). Needs restructuring next session: likely split into a thin Server Component page that only exports `generateStaticParams()` (returning the `{ id: "_" }` placeholder), rendering a separate `"use client"` child component that does the actual `window.location.pathname`-parsing + `api.getPlaylist()` fetch + render. Not fixed this session — flagged in `TODO.md`.
